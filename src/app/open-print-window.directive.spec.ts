import { OpenPrintWindowDirective } from './open-print-window.directive';

describe('OpenPrintWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenPrintWindowDirective();
    expect(directive).toBeTruthy();
  });
});
